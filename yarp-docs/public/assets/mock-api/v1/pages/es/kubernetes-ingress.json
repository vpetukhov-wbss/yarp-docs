{
  "slug": "kubernetes-ingress",
  "locale": "es",
  "group": "deployment",
  "kind": "doc",
  "title": "Controlador de Ingress de Kubernetes",
  "lede": "YARP puede integrarse con Kubernetes como un proxy inverso que gestiona el tráfico HTTP/HTTPS de",
  "headings": [
    {
      "id": "prerequisites",
      "text": "Requisitos previos",
      "level": 2
    },
    {
      "id": "get-started",
      "text": "Primeros pasos",
      "level": 2
    },
    {
      "id": "kubectl-apply-f-ingress-controller-yaml",
      "text": "kubectl apply -f ingress-controller.yaml",
      "level": 2
    },
    {
      "id": "kubectl-get-pods-n-yarp",
      "text": "kubectl get pods -n yarp",
      "level": 2
    },
    {
      "id": "kubectl-apply-f-ingress-yaml",
      "text": "kubectl apply -f .\\ingress.yaml",
      "level": 2
    },
    {
      "id": "deploying-an-app",
      "text": "Implementación de una aplicación",
      "level": 2
    },
    {
      "id": "kubectl-apply-f-backend-yaml",
      "text": "kubectl apply -f .\\backend.yaml",
      "level": 2
    },
    {
      "id": "creating-the-ingress-definition",
      "text": "Creación de la definición del Ingress",
      "level": 2
    },
    {
      "id": "kubectl-apply-f-ingress-sample-yaml",
      "text": "kubectl apply -f .\\ingress-sample.yaml",
      "level": 2
    },
    {
      "id": "kubectl-get-service-n-yarp",
      "text": "kubectl get service -n yarp",
      "level": 2
    }
  ],
  "bodyHtml": "<p>Introducido: versión preliminar futura.</p>\n<p>YARP puede integrarse con Kubernetes como un proxy inverso que gestiona la entrada (ingress) de tráfico HTTP/HTTPS a un clúster de Kubernetes. Actualmente, el módulo se distribuye como un paquete independiente y se encuentra en versión preliminar (preview).</p>\n<h2 id=\"prerequisites\">Requisitos previos</h2><p>Antes de continuar con este tutorial, asegúrese de tener preparado lo siguiente...</p>\n<ol>\n<li>Instalar Docker según su sistema operativo.</li>\n<li>Un registro de contenedores. De forma predeterminada, Docker crea un registro de contenedores en DockerHub. También puede usar Azure Container Registry u otro registro de contenedores de su elección, como un registro local para pruebas.</li>\n<li>Un clúster de Kubernetes. Hay muchas opciones disponibles, entre ellas:</li>\n</ol>\n<p>Azure Kubernetes Service. Kubernetes en Docker Desktop, aunque este consume bastante memoria en su equipo, así que úselo con precaución. Minikube. K3s, una distribución de Kubernetes certificada, ligera y de un solo binario, de Rancher. Otro proveedor de Kubernetes de su elección.</p>\n<p>Nota</p>\n<p>Si elige un registro de contenedores proporcionado por un proveedor de nube distinto de Dockerhub, probablemente deba realizar pasos adicionales para configurar su clúster de Kubernetes y permitir el acceso. Siga las instrucciones proporcionadas por su proveedor de nube.</p>\n<h2 id=\"get-started\">Primeros pasos</h2><div class=\"callout\"><svg width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><circle cx=\"12\" cy=\"12\" r=\"9\"/><path d=\"M12 8h.01M11 12h1v5h1\"/></svg><div><div class=\"head\">Nota</div><p>Por ahora, no existe una imagen oficial de Docker para el controlador de Ingress de YARP.</p>\n</div></div><p>Mientras tanto, el controlador de Ingress de YARP debe compilarse localmente e implementarse. En la raíz del repositorio, ejecute:</p>\n<p><code>docker build . -t {REGISTRY_NAME}/yarp-controller:{TAG} -f .\\src\\Kubernetes.Controller\\Dockerfile</code>\n<code>docker push {REGISTRY_NAME}/yarp-controller:{TAG}</code></p>\n<p>En los comandos anteriores, el marcador de posición <code>{REGISTRY_NAME}</code> es el nombre del registro de Docker, y el marcador de posición <code>{TAG}</code> es una etiqueta para la imagen (por ejemplo, <code>1.0.0</code>).</p>\n<p>El primer paso es implementar el controlador de Ingress de YARP en el clúster de Kubernetes. Para ello, vaya a la muestra de Kubernetes Ingress en <code>\\samples\\KubernetesIngress.Sample\\Ingress</code> y ejecute (después de modificar <code>ingress-controller.yaml</code> con el mismo <code>REGISTRY_NAME</code> y <code>TAG</code>):</p>\n<h2 id=\"kubectl-apply-f-ingress-controller-yaml\">kubectl apply -f ingress-controller.yaml</h2><p>Para comprobar que el controlador de Ingress se ha implementado, ejecute:</p>\n<h2 id=\"kubectl-get-pods-n-yarp\">kubectl get pods -n yarp</h2><p>A continuación, puede revisar los registros del controlador de Ingress ejecutando:</p>\n<p><code>kubectl logs {POD NAME} -n yarp</code></p>\n<p>Todos los servicios, implementaciones y pods de YARP se encuentran en el espacio de nombres <code>yarp</code>. Asegúrese de incluir <code>-n yarp</code> si desea comprobar el estado de yarp. A continuación, compile e implemente el Ingress. En la raíz del repositorio, ejecute:</p>\n<p><code>docker build . -t {REGISTRY_NAME}/yarp:&lt;TAG&gt; -f .\\samples\\KuberenetesIngress.Sample\\Ingress\\Dockerfile</code>\n<code>docker push {REGISTRY_NAME}/yarp:{TAG}</code></p>\n<p>En los comandos anteriores, el marcador de posición <code>{REGISTRY_NAME}</code> es el nombre del registro de Docker, y el marcador de posición <code>{TAG}</code> es una etiqueta para la imagen (por ejemplo, <code>1.0.0</code>).</p>\n<p>Por último, es necesario implementar el propio Ingress en Kubernetes. Vaya al directorio Ingress, modifique el archivo <code>ingress.yaml</code> con el registro y la etiqueta indicados anteriormente, y ejecute:</p>\n<h2 id=\"kubectl-apply-f-ingress-yaml\">kubectl apply -f .\\ingress.yaml</h2><p>En este punto, el Ingress y el controlador deberían estar en ejecución.</p>\n<h2 id=\"deploying-an-app\">Implementación de una aplicación</h2><p>Para usar el Ingress, ahora es necesario implementar una aplicación en Kubernetes. Vaya a <code>samples\\KuberenetesIngress.Sample\\backend</code> y ejecute:</p>\n<p><code>docker build . -t {REGISTRY_NAME}/backend:{TAG}</code>\n<code>docker push {REGISTRY_NAME}/backend:{TAG}</code></p>\n<p>Y despliéguela en Kubernetes ejecutando, después de modificar <code>backend.yaml</code> con el mismo nombre de registro (<code>{REGISTRY_NAME}</code>) y etiqueta (<code>{TAG}</code>):</p>\n<h2 id=\"kubectl-apply-f-backend-yaml\">kubectl apply -f .\\backend.yaml</h2><h2 id=\"creating-the-ingress-definition\">Creación de la definición del Ingress</h2><p>Por último, una vez implementada la aplicación de back-end, es necesario enrutar el tráfico hacia ella. Para ello, ejecute en el directorio backend:</p>\n<h2 id=\"kubectl-apply-f-ingress-sample-yaml\">kubectl apply -f .\\ingress-sample.yaml</h2><p>A continuación, ejecute el siguiente comando para obtener la IP externa del Ingress; el nombre del servicio relacionado es <code>yarp-proxy</code>:</p>\n<h2 id=\"kubectl-get-service-n-yarp\">kubectl get service -n yarp</h2><p>Si usa un clúster de K8s local y no obtiene una IP externa, es posible que deba evitar usar el puerto predeterminado, el 80, para el servicio <code>yarp-proxy</code>. En ese caso, intente actualizar de nuevo el archivo <code>ingress.yaml</code> para usar otro puerto (por ejemplo, <code>port: 8085</code>) y vuelva a implementar el Ingress en Kubernetes.</p>\n<p>Vaya a la IP externa y debería ver la información del back-end.</p>\n<div class=\"callout\"><svg width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><circle cx=\"12\" cy=\"12\" r=\"9\"/><path d=\"M12 8h.01M11 12h1v5h1\"/></svg><div><div class=\"head\">Nota</div><p>El autor creó este artículo con la ayuda de inteligencia artificial. Más información</p>\n</div></div>",
  "sourceUrl": "https://learn.microsoft.com/en-us/aspnet/core/fundamentals/servers/yarp/kubernetes-ingress",
  "lastUpdated": "2026-08-11",
  "translated": true,
  "prev": {
    "slug": "ab-testing",
    "title": "Pruebas A/B y actualizaciones progresivas"
  },
  "next": {
    "slug": "service-fabric-int",
    "title": "Integración con Service Fabric"
  }
}

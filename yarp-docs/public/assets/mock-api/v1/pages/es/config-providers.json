{
  "slug": "config-providers",
  "locale": "es",
  "group": "configuration",
  "kind": "doc",
  "title": "Configuration providers",
  "lede": "Load routes and clusters programmatically instead of from a file, by implementing IProxyConfigProvider yourself - useful for a database, a remote API, or any other source.",
  "headings": [
    {
      "id": "the-provider-interface",
      "text": "The provider interface",
      "level": 2
    },
    {
      "id": "in-memory",
      "text": "Loading routes and clusters directly",
      "level": 2
    },
    {
      "id": "lifecycle",
      "text": "Provider lifecycle",
      "level": 2
    },
    {
      "id": "startup",
      "text": "Startup",
      "level": 3
    },
    {
      "id": "reloading",
      "text": "Reloading",
      "level": 3
    },
    {
      "id": "multiple-providers",
      "text": "Multiple providers",
      "level": 2
    }
  ],
  "bodyHtml": "<h2 id=\"the-provider-interface\">The provider interface</h2><p><a href=\"/es/config-files\">Configuration files</a> cover the common case of loading from <code>IConfiguration</code>. To load from anywhere else, implement <code>IProxyConfigProvider</code> and <code>IProxyConfig</code> yourself.</p>\n<p><code>IProxyConfigProvider</code> has a single method, <code>GetConfig()</code>, returning an <code>IProxyConfig</code> - a snapshot with the current routes and clusters, plus an <code>IChangeToken</code> the provider signals whenever that snapshot is out of date, which causes the proxy to call <code>GetConfig()</code> again.</p>\n<h2 id=\"in-memory\">Loading routes and clusters directly</h2><p>For the simplest case - routes and clusters known entirely in code - <code>InMemoryConfigProvider</code> is a ready-made <code>IProxyConfigProvider</code>:</p>\n<pre class=\"code-block standalone\"><span class=\"lang-tag\">C#</span><button class=\"copy-btn\" data-copy=\"config-providers-code-1\" aria-label=\"Copiar código\"><svg class=\"copy\" width=\"14\" height=\"14\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><rect x=\"9\" y=\"9\" width=\"12\" height=\"12\" rx=\"2\"/><path d=\"M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1\"/></svg><svg class=\"check\" width=\"14\" height=\"14\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><path d=\"M20 6 9 17l-5-5\"/></svg></button><code id=\"config-providers-code-1\">services<span class=\"tok-punc\">.</span><span class=\"tok-type\">AddReverseProxy</span><span class=\"tok-punc\">(</span><span class=\"tok-punc\">)</span><span class=\"tok-punc\">.</span><span class=\"tok-type\">LoadFromMemory</span><span class=\"tok-punc\">(</span>routes<span class=\"tok-punc\">,</span> clusters<span class=\"tok-punc\">)</span><span class=\"tok-punc\">;</span></code></pre><p>To change that configuration later, resolve <code>InMemoryConfigProvider</code> from the service container and call <code>Update</code>:</p>\n<pre class=\"code-block standalone\"><span class=\"lang-tag\">C#</span><button class=\"copy-btn\" data-copy=\"config-providers-code-2\" aria-label=\"Copiar código\"><svg class=\"copy\" width=\"14\" height=\"14\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><rect x=\"9\" y=\"9\" width=\"12\" height=\"12\" rx=\"2\"/><path d=\"M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1\"/></svg><svg class=\"check\" width=\"14\" height=\"14\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><path d=\"M20 6 9 17l-5-5\"/></svg></button><code id=\"config-providers-code-2\">httpContext<span class=\"tok-punc\">.</span>RequestServices<span class=\"tok-punc\">.</span><span class=\"tok-type\">GetRequiredService</span><span class=\"tok-punc\">&lt;</span>InMemoryConfigProvider<span class=\"tok-punc\">&gt;</span><span class=\"tok-punc\">(</span><span class=\"tok-punc\">)</span>\n    <span class=\"tok-punc\">.</span><span class=\"tok-type\">Update</span><span class=\"tok-punc\">(</span>routes<span class=\"tok-punc\">,</span> clusters<span class=\"tok-punc\">)</span><span class=\"tok-punc\">;</span></code></pre><h2 id=\"lifecycle\">Provider lifecycle</h2><h3 id=\"startup\">Startup</h3><p><code>IProxyConfigProvider</code> is registered as a singleton. At startup, the proxy resolves it and calls <code>GetConfig()</code> once; the provider can:</p>\n<ul>\n<li>throw, if it can&#39;t produce valid configuration - this prevents the application from starting;</li>\n<li>block synchronously until the configuration is loaded, which delays startup until valid route data is available; or</li>\n<li>return an empty <code>IProxyConfig</code> immediately and load in the background, signaling its <code>IChangeToken</code> once real data is ready.</li>\n</ul>\n<p>Whatever configuration is returned is validated, and an invalid result throws an exception that prevents startup - a provider can pre-validate with <code>IConfigValidator</code> first and exclude invalid entries itself instead.</p>\n<p>Route and cluster objects handed to the proxy should be treated as read-only once returned from <code>GetConfig()</code>.</p>\n<h3 id=\"reloading\">Reloading</h3><p>If the <code>IChangeToken</code> supports active change callbacks, the proxy registers one after the initial load; otherwise <code>HasChanged</code> is polled every 5 minutes. To publish a new configuration, a provider should load it in the background - building new route/cluster instances, since they&#39;re immutable, though unchanged ones can be reused - optionally validate it, and only then signal the <em>previous</em> <code>IChangeToken</code>. The proxy calls <code>GetConfig()</code> again in response and diffs the result against the current configuration, updating only what changed; the swap is atomic and only affects new requests, not ones already in flight.</p>\n<div class=\"callout callout-important\"><svg width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><path d=\"M12 9v4M12 17h.01\"/><path d=\"M10.3 3.9 1.8 18a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 3.9a2 2 0 0 0-3.4 0Z\"/></svg><div><div class=\"head\">Importante</div><p><code>IChangeToken</code>s are single-use. If <code>GetConfig()</code> throws during a reload, the proxy loses its ability to listen for further changes from that provider. Any other reload errors are logged and suppressed instead, and the proxy keeps using the last known good configuration.</p>\n</div></div><p>If several reloads are signaled in quick succession, the proxy may skip some and load whatever is available by the time it catches up - each <code>IProxyConfig</code> is a full snapshot, not a diff, so nothing is lost by skipping an intermediate one.</p>\n<h2 id=\"multiple-providers\">Multiple providers</h2><p>More than one <code>IProxyConfigProvider</code> can be registered as a singleton; all of them are resolved and their configuration combined, the same way multiple <a href=\"/es/config-files\">configuration file</a> sections can be. A route from one provider can reference a cluster from another, but a single route or cluster can&#39;t be assembled from partial data spread across two providers.</p>\n",
  "sourceUrl": "https://learn.microsoft.com/en-us/aspnet/core/fundamentals/servers/yarp/config-providers",
  "lastUpdated": "2025-02-10",
  "translated": false,
  "prev": {
    "slug": "config-filters",
    "title": "Configuration filters"
  },
  "next": {
    "slug": "http-client-config",
    "title": "HTTP client configuration"
  }
}

{
  "slug": "destination-resolvers",
  "locale": "es",
  "group": "extensibility",
  "kind": "doc",
  "title": "Solucionadores de destinos",
  "lede": "YARP usa un solucionador de destinos para expandir el conjunto de direcciones de destino configuradas. El",
  "headings": [
    {
      "id": "yarp-extensibility-destination-resolvers",
      "text": "Extensibilidad de YARP: solucionadores de destinos",
      "level": 2
    },
    {
      "id": "introduction",
      "text": "Introducción",
      "level": 2
    },
    {
      "id": "structure",
      "text": "Estructura",
      "level": 2
    },
    {
      "id": "idestinationresolver-has-a-single-method",
      "text": "IDestinationResolver tiene un único método",
      "level": 2
    },
    {
      "id": "destinationconfig",
      "text": "DestinationConfig",
      "level": 2
    },
    {
      "id": "lifecycle",
      "text": "Ciclo de vida",
      "level": 2
    },
    {
      "id": "startup",
      "text": "Inicio",
      "level": 2
    },
    {
      "id": "atomicity",
      "text": "Atomicidad",
      "level": 2
    },
    {
      "id": "reload",
      "text": "Recarga",
      "level": 2
    },
    {
      "id": "dns-destination-resolver",
      "text": "Solucionador de destinos DNS",
      "level": 2
    },
    {
      "id": "example",
      "text": "Ejemplo",
      "level": 2
    },
    {
      "id": "configuration",
      "text": "Configuración",
      "level": 2
    },
    {
      "id": "refreshperiod",
      "text": "RefreshPeriod",
      "level": 2
    },
    {
      "id": "addressfamily",
      "text": "AddressFamily",
      "level": 2
    }
  ],
  "bodyHtml": "<h2 id=\"yarp-extensibility-destination-resolvers\">Extensibilidad de YARP: solucionadores de destinos</h2><h2 id=\"introduction\">Introducción</h2><p>YARP usa un solucionador de destinos para expandir el conjunto de direcciones de destino configuradas. El solucionador de destinos puede usarse como punto de integración con sistemas de detección de servicios (service discovery).</p>\n<h2 id=\"structure\">Estructura</h2><h2 id=\"idestinationresolver-has-a-single-method\">IDestinationResolver tiene un único método</h2><p><code>ResolveDestinationsAsync(IReadOnlyDictionary&lt;string, DestinationConfig&gt; destinations, CancellationToken cancellationToken)</code>, que debe devolver una instancia de <code>ResolvedDestinationCollection</code>. Esta clase contiene una colección de instancias de <code>DestinationConfig</code>, además de un <code>IChangeToken</code> que notifica al proxy cuando la información queda obsoleta y debe recargarse; esto hace que <code>ResolveDestinationsAsync</code> se vuelva a invocar.</p>\n<h2 id=\"destinationconfig\">DestinationConfig</h2><p><code>DestinationConfig</code> expone una propiedad <code>Host</code> que permite especificar el valor de encabezado <code>Host</code> predeterminado que el proxy debe usar al comunicarse con ese destino. Esto permite que <code>IDestinationResolver</code> resuelva destinos hacia una colección de direcciones IP, por ejemplo, sin que el enrutamiento basado en SNI o en el host deje de funcionar.</p>\n<h2 id=\"lifecycle\">Ciclo de vida</h2><h2 id=\"startup\">Inicio</h2><p><code>IDestinationResolver</code> debe registrarse en el contenedor de inyección de dependencias como singleton. Al iniciarse, el proxy resolverá esta instancia y llamará a <code>ResolveDestinationsAsync(...)</code> con los destinos configurados, obtenidos de los <code>IProxyConfigProvider</code> resueltos. En esta primera llamada, el proveedor puede optar por:</p>\n<ul>\n<li>Generar una excepción si no puede producir una configuración de proxy válida por cualquier motivo; esto impedirá que la aplicación se inicie.</li>\n<li>Resolver los destinos de forma asincrónica; esto impedirá que la aplicación termine de iniciarse hasta que los destinos resueltos estén disponibles.</li>\n<li>O bien, devolver una instancia vacía de <code>ResolvedDestinationCollection</code> mientras resuelve los destinos en segundo plano; en ese caso, el proveedor deberá activar el <code>IChangeToken</code> cuando la configuración esté disponible.</li>\n</ul>\n<h2 id=\"atomicity\">Atomicidad</h2><p>Los objetos y las colecciones de destinos que se entregan al proxy deben ser de solo lectura y no deben modificarse una vez entregados a través de <code>GetConfig()</code>.</p>\n<h2 id=\"reload\">Recarga</h2><p>Si el <code>IChangeToken</code> admite <code>ActiveChangeCallbacks</code>, una vez que el proxy haya procesado el conjunto inicial de destinos registrará una devolución de llamada (callback) con ese token. Si el proveedor no admite devoluciones de llamada, <code>HasChanged</code> se sondeará junto con los tokens de cambio de <code>IProxyConfig</code>, cada 5 minutos.</p>\n<p>Cuando el proveedor quiera proporcionar un nuevo conjunto de destinos al proxy, debe:</p>\n<ul>\n<li>Resolver esos destinos en segundo plano. <code>ResolvedDestinationCollection</code> es inmutable, por lo que deben crearse instancias nuevas para cualquier dato nuevo; los objetos de los destinos que no hayan cambiado pueden reutilizarse, o bien pueden crearse instancias nuevas.</li>\n<li>Invalidar el <code>IChangeToken</code> devuelto por la invocación anterior de <code>ResolveDestinationsAsync</code>.</li>\n</ul>\n<p>Una vez aplicados los nuevos destinos, el proxy registrará una devolución de llamada con el nuevo <code>IChangeToken</code>. Tenga en cuenta que, si se señalan varias recargas en rápida sucesión, el proxy puede omitir algunas de ellas y resolver los destinos en cuanto esté listo.</p>\n<h2 id=\"dns-destination-resolver\">Solucionador de destinos DNS</h2><p>YARP incluye una implementación de <code>IDestinationResolver</code> que expande el conjunto de destinos configurados resolviendo cada nombre de host a una o varias direcciones IP mediante DNS, creando un destino por cada IP resuelta. El solucionador de destinos DNS puede agregarse al proxy inverso mediante el método <code>IReverseProxyBuilder.AddDnsDestinationResolver(Action&lt;DnsDestinationResolverOptions&gt;)</code>. El método acepta un delegado opcional para configurar las opciones del solucionador, <code>DnsDestinationResolverOptions</code>.</p>\n<h2 id=\"example\">Ejemplo</h2><pre class=\"code-block standalone\"><span class=\"lang-tag\">C#</span><button class=\"copy-btn\" data-copy=\"destination-resolvers-code-1\" aria-label=\"Copiar código\"><svg class=\"copy\" width=\"14\" height=\"14\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><rect x=\"9\" y=\"9\" width=\"12\" height=\"12\" rx=\"2\"/><path d=\"M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1\"/></svg><svg class=\"check\" width=\"14\" height=\"14\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><path d=\"M20 6 9 17l-5-5\"/></svg></button><code id=\"destination-resolvers-code-1\"><span class=\"tok-com\">// Add the DNS destination resolver, restricting results to IPv4 addresses</span>\nreverseProxyBuilder<span class=\"tok-punc\">.</span><span class=\"tok-type\">AddDnsDestinationResolver</span><span class=\"tok-punc\">(</span>o <span class=\"tok-punc\">=&gt;</span> o<span class=\"tok-punc\">.</span>AddressFamily <span class=\"tok-punc\">=</span>\nAddressFamily<span class=\"tok-punc\">.</span>InterNetwork<span class=\"tok-punc\">)</span><span class=\"tok-punc\">;</span></code></pre><h2 id=\"configuration\">Configuración</h2><p>Las opciones del solucionador de destinos DNS, <code>DnsDestinationResolverOptions</code>, exponen las siguientes propiedades:</p>\n<h2 id=\"refreshperiod\">RefreshPeriod</h2><p>El período entre solicitudes de actualización de un nombre resuelto. El valor predeterminado es 5 minutos.</p>\n<h2 id=\"addressfamily\">AddressFamily</h2><p>Opcionalmente, especifique un valor <code>System.Net.Sockets.AddressFamily</code> de <code>AddressFamily.InterNetwork</code> o <code>AddressFamily.InterNetworkV6</code> para restringir la resolución a direcciones IPv4 o IPv6, respectivamente. El valor predeterminado, <code>null</code>, indica al solucionador que no restrinja la familia de direcciones de los resultados y que acepte todas las direcciones devueltas.</p>\n<div class=\"callout\"><svg width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><circle cx=\"12\" cy=\"12\" r=\"9\"/><path d=\"M12 8h.01M11 12h1v5h1\"/></svg><div><div class=\"head\">Nota</div><p>El autor creó este artículo con la ayuda de inteligencia artificial. Más información</p>\n</div></div>",
  "sourceUrl": "https://learn.microsoft.com/en-us/aspnet/core/fundamentals/servers/yarp/destination-resolvers",
  "lastUpdated": "2026-08-11",
  "translated": true,
  "prev": {
    "slug": "direct-forwarding",
    "title": "Reenvío directo"
  },
  "next": {
    "slug": "extensibility-transforms",
    "title": "Transformaciones de solicitud y respuesta"
  }
}

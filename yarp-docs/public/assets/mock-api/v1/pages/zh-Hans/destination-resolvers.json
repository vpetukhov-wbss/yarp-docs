{
  "slug": "destination-resolvers",
  "locale": "zh-Hans",
  "group": "extensibility",
  "kind": "doc",
  "title": "Destination resolvers",
  "lede": "YARP uses a destination resolver to expand the set of configured destination addresses. The",
  "headings": [
    {
      "id": "yarp-extensibility-destination-resolvers",
      "text": "YARP Extensibility Destination Resolvers",
      "level": 2
    },
    {
      "id": "introduction",
      "text": "Introduction",
      "level": 2
    },
    {
      "id": "structure",
      "text": "Structure",
      "level": 2
    },
    {
      "id": "idestinationresolver-has-a-single-method",
      "text": "IDestinationResolver has a single method",
      "level": 2
    },
    {
      "id": "destinationconfig",
      "text": "DestinationConfig",
      "level": 2
    },
    {
      "id": "lifecycle",
      "text": "Lifecycle",
      "level": 2
    },
    {
      "id": "startup",
      "text": "Startup",
      "level": 2
    },
    {
      "id": "atomicity",
      "text": "Atomicity",
      "level": 2
    },
    {
      "id": "reload",
      "text": "Reload",
      "level": 2
    },
    {
      "id": "dns-destination-resolver",
      "text": "DNS Destination Resolver",
      "level": 2
    },
    {
      "id": "example",
      "text": "Example",
      "level": 2
    },
    {
      "id": "configuration",
      "text": "Configuration",
      "level": 2
    },
    {
      "id": "refreshperiod",
      "text": "RefreshPeriod",
      "level": 2
    },
    {
      "id": "addressfamily",
      "text": "AddressFamily",
      "level": 2
    }
  ],
  "bodyHtml": "<h2 id=\"yarp-extensibility-destination-resolvers\">YARP Extensibility Destination Resolvers</h2><h2 id=\"introduction\">Introduction</h2><p>YARP uses a destination resolver to expand the set of configured destination addresses. The destination resolver can be used as an integration point with service discovery systems.</p>\n<h2 id=\"structure\">Structure</h2><h2 id=\"idestinationresolver-has-a-single-method\">IDestinationResolver has a single method</h2><p>ResolveDestinationsAsync(IReadOnlyDictionary&lt;string, DestinationConfig&gt; destinations,</p>\n<p>CancellationToken cancellationToken) which should return a ResolvedDestinationCollection instance. The ResolvedDestinationCollection has a collection of DestinationConfig instances, as well as an IChangeToken to notify the proxy when this information is out of date and should be reloaded, which will cause ResolveDestinationsAsync to be called again.</p>\n<h2 id=\"destinationconfig\">DestinationConfig</h2><p>DestinationConfig has a Host property which can be used to specify the default Host header value which the proxy should use when communicating with that destination. This allows the IDestinationResolver to resolve destinations to a collection of IP addresses, for example, without causing SNI or host-based routing to fail.</p>\n<h2 id=\"lifecycle\">Lifecycle</h2><h2 id=\"startup\">Startup</h2><p>The IDestinationResolver should be registered in the DI container as a singleton. At startup, the proxy will resolve this instance and call ResolveDestinationsAsync(...) with the configured destinations retrieved from the resolved IProxyConfigProviders . On this first call the provider may choose to:</p>\n<p>Throw an exception if the provider cannot produce a valid proxy configuration for any reason. This will prevent the application from starting. Asynchronously resolve the destinations. This will stop the application from starting until resolved destinations are available.</p>\n<p>Or, it may choose to return an empty ResolvedDestinationCollection instance while it</p>\n<p>resolves destinations in the background. The provider will need to trigger the</p>\n<p>IChangeToken when the configuration is available.</p>\n<h2 id=\"atomicity\">Atomicity</h2><p>The destinations objects and collections supplied to the proxy should be read-only and not modified once they have been handed to the proxy via GetConfig() .</p>\n<h2 id=\"reload\">Reload</h2><p>If the IChangeToken supports ActiveChangeCallbacks , once the proxy has processed the initial set of destinations it will register a callback with this token. If the provider does not support callbacks then HasChanged will be polled alongside IProxyConfig change tokens, every 5 minutes.</p>\n<p>When the provider wants to provide a new set of destinations to the proxy it should:</p>\n<p>Resolve those destinations in the background. ResolvedDestinationCollection is immutable, so new instances have to be created for any new data. Objects for unchanged destinations can be re-used, or new instances can be created.</p>\n<p>Invalidate the IChangeToken returned from the previous ResolveDestinationsAsync invocation.</p>\n<p>Once the new destinations have been applied, the proxy will register a callback with the new IChangeToken . Note if there are multiple reloads signaled in close succession, the proxy may skip some and resolve destinations as soon as it&#39;s ready.</p>\n<h2 id=\"dns-destination-resolver\">DNS Destination Resolver</h2><p>YARP includes an IDestinationResolver implementation which expands the set of configured destinations by resolving each host name to one or more IP addresses using DNS, creating a destination for each resolved IP. The DNS destination resolver can be added to your reverse proxy using the</p>\n<p>IReverseProxyBuilder.AddDnsDestinationResolver(Action<DnsDestinationResolverOptions>)</p>\n<p>method. The method accepts an optional delegate to configure the resolver&#39;s options, DnsDestinationResolverOptions.</p>\n<h2 id=\"example\">Example</h2><pre class=\"code-block standalone\"><span class=\"lang-tag\">C#</span><button class=\"copy-btn\" data-copy=\"destination-resolvers-code-1\" aria-label=\"复制代码\"><svg class=\"copy\" width=\"14\" height=\"14\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><rect x=\"9\" y=\"9\" width=\"12\" height=\"12\" rx=\"2\"/><path d=\"M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1\"/></svg><svg class=\"check\" width=\"14\" height=\"14\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><path d=\"M20 6 9 17l-5-5\"/></svg></button><code id=\"destination-resolvers-code-1\"><span class=\"tok-com\">// Add the DNS destination resolver, restricting results to IPv4 addresses</span>\nreverseProxyBuilder<span class=\"tok-punc\">.</span><span class=\"tok-type\">AddDnsDestinationResolver</span><span class=\"tok-punc\">(</span>o <span class=\"tok-punc\">=&gt;</span> o<span class=\"tok-punc\">.</span>AddressFamily <span class=\"tok-punc\">=</span>\nAddressFamily<span class=\"tok-punc\">.</span>InterNetwork<span class=\"tok-punc\">)</span><span class=\"tok-punc\">;</span></code></pre><h2 id=\"configuration\">Configuration</h2><p>The DNS destination resolver&#39;s options, DnsDestinationResolverOptions, has the following properties:</p>\n<h2 id=\"refreshperiod\">RefreshPeriod</h2><p>The period between requesting a refresh of a resolved name. This defaults to 5 minutes.</p>\n<h2 id=\"addressfamily\">AddressFamily</h2><p>Optionally, specify an System.Net.Sockets.AddressFamily value of AddressFamily.InterNetwork or AddressFamily.InterNetworkV6 to restrict resolved resolution to IPv4 or IPv6 addresses, respectively. The default value, null , instructs the resolver to not restrict the address family of the results and to use accept all returned addresses.</p>\n<div class=\"callout\"><svg width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><circle cx=\"12\" cy=\"12\" r=\"9\"/><path d=\"M12 8h.01M11 12h1v5h1\"/></svg><div><div class=\"head\">说明</div><p>The author created this article with assistance from AI. Learn more</p>\n</div></div>",
  "sourceUrl": "https://learn.microsoft.com/en-us/aspnet/core/fundamentals/servers/yarp/destination-resolvers",
  "lastUpdated": "2026-08-11",
  "translated": false,
  "prev": {
    "slug": "direct-forwarding",
    "title": "Direct forwarding"
  },
  "next": {
    "slug": "extensibility-transforms",
    "title": "Request & response transforms"
  }
}
